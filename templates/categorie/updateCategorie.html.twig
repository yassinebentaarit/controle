
{% extends 'base.html.twig'%}

{% block body %}
    <div style= "display: flex; align-items: center;justify-content:center; margin-top:100px;padding:10px;">

      <div class="card">
        <div class="card-body">
          <h5 class="card-title">AJOUT D'UNE CATEGORIE</h5>

          <!-- General Form Elements -->
          {{ form_start(f,{attr:{novalidate:'novalidate'}}) }}
          <div class="row mb-10 col-sm-10">
            <label for="inputText" class="col-sm-6 col-form-label">Nom du categorie</label>
              <div class="col-sm-5">
                {{ form_widget(f.libelle,{'attr':{'class':'form-control'}}) }}

              </div>
            </div>

                <div class="form-check">
                {{ form_widget(f.type, {'type':"checkbox", 'class':"form-check-label"}) }}
                          
  
                        Cocher si c'est un médicament
                        </div>


                  
          <div class="col-sm-10" style="display:flex; align-items:center; justify-content:center">
              
            {{ form_widget(f.Ajouter,{'attr':{'class':'btn btn-primary'}}) }}
          </div>

        {{ form_end(f) }}

      </div>
    </div>

  </div> 

{% endblock %}